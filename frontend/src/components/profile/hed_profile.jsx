import Logo from "../headers/Logo";
import ProfileNav from "../headers/profile-nav";

const HedProfile = () => {
  return (
    <div className="header">
      <Logo />
      <ProfileNav />
    </div>
  );
};

export default HedProfile;
